# 切換客戶步驟（SaaS 部署指南）

## 架構說明

每個企業客戶 = 一個 GitHub 專案 + 一個 Supabase 專案。
程式碼相同，只有 `config.js` 的 2 個值不同。
**企業名稱、Logo、說明文字等品牌資訊全部從資料庫自動讀取**，改資料庫就會自動生效。

---

## Step 1：建立新的 Supabase 專案

1. 到 [Supabase Dashboard](https://supabase.com/dashboard) 建立新專案
2. 區域建議選 `ap-northeast-1`（東京，離台灣最近）
3. 記下專案的 **URL** 和 **anon key**（在 Settings → API 可找到）

## Step 2：套用資料庫 Schema

在新專案的 SQL Editor 執行以下步驟：

1. 執行 `database.md` 中的所有建表 SQL（users、questions、user_question_progress、answer_records、random_test_sessions、chapter_access、tags 等）
2. 執行 `sql/` 資料夾內的所有 RPC 函數：
   - `admin_dashboard_functions.sql`
   - `admin_attempt_accuracy.sql`
   - `get_user_daily_streak.sql`
3. 建立 `organization_settings` 表並插入該企業的品牌資料：

```sql
CREATE TABLE IF NOT EXISTS public.organization_settings (
    id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    org_name text NOT NULL,
    short_name text NOT NULL,
    description text DEFAULT '',
    footer_text text DEFAULT '',
    app_version text DEFAULT 'v1.0.0',
    primary_color text DEFAULT '#FFD600',
    logo_url text DEFAULT '',
    created_at timestamptz DEFAULT now(),
    updated_at timestamptz DEFAULT now()
);

ALTER TABLE public.organization_settings ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Allow public read" ON public.organization_settings FOR SELECT USING (true);
CREATE POLICY "Allow admin update" ON public.organization_settings FOR UPDATE USING (
    EXISTS (SELECT 1 FROM public.users WHERE id = auth.uid() AND role IN ('master_admin', 'super_admin'))
);

-- ⬇️ 改成新企業的資料
INSERT INTO public.organization_settings (org_name, short_name, description, footer_text, logo_url)
VALUES (
    '○○企業名稱',
    '○○簡稱',
    '○○企業證照考試練習平台',
    '○○企業名稱 考試系統 v1.0.0',
    'https://xxx.com/logo.png'   -- Logo 圖片網址（可留空）
);
```

4. 設定 RLS 政策（確保所有表都有啟用）
5. 在 `users` 表手動建立一個 `master_admin` 帳號

## Step 3：Fork GitHub 專案 & 修改 config.js

1. Fork 主專案到新的 GitHub repo
2. 打開 `config.js`，修改最上方的 **2 個值**：

```javascript
const SUPABASE_CONFIG = {
    url: 'https://○○○○○○○○.supabase.co',   // ← 新專案的 URL
    anonKey: '○○○○○○○○'                     // ← 新專案的 anon key
};
```

**只改這 2 行，其他檔案完全不用動。**
企業名稱、Logo 等品牌資訊會自動從資料庫的 `organization_settings` 表讀取。

## Step 4：部署

用 GitHub Pages、Netlify、Vercel 或任何靜態主機部署即可。

## Step 5：設定 Supabase Auth（如果有用 Google 登入）

到新 Supabase 專案 → Authentication → URL Configuration：
- Site URL 設為新網站的網址（例如 `https://xxx.netlify.app`）
- Redirect URLs 加上 `https://xxx.netlify.app/**`

---

## 之後要改企業資訊？

直接到 Supabase Dashboard 修改 `organization_settings` 表即可，不用動程式碼：

| 欄位 | 用途 | 範例 |
|------|------|------|
| `org_name` | 企業全名（頁首、登入頁、頁面標題） | 台北職業訓練中心 |
| `short_name` | 簡稱（PWA 圖示下方文字） | 台北考試 |
| `description` | 說明文字 | 企業證照考試練習平台 |
| `footer_text` | 頁尾文字 | 台北職訓中心 v1.0.0 |
| `logo_url` | Logo 圖片網址 | https://xxx.com/logo.png |
| `primary_color` | 品牌主色 | #FFD600 |

改完存檔，重新整理網頁就會看到新的資訊。

---

## 快速 Checklist

- [ ] Supabase 新專案已建立
- [ ] 所有資料表 + RPC 函數已建立
- [ ] `organization_settings` 已插入企業品牌資料
- [ ] `master_admin` 帳號已建立
- [ ] `config.js` 的 2 個值已修改（url + anonKey）
- [ ] 網站已部署並可正常登入
- [ ] （如需要）Google OAuth redirect URL 已設定
